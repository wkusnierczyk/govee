/// A workflow definition (timed sequence of device commands).
pub struct Workflow;

/// A single step in a workflow.
pub struct Step;

/// Target for a workflow step (single device, group, or all).
pub struct Target;
